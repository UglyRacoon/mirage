# runtime data (SQLite db, per-profile kernel dirs, secrets, cookies)
data/

# deps / OS cruft
node_modules/
.DS_Store
*.log
npm-debug.log*

# local overrides
.env
*.local

# Local security-audit report (public repo — kept out of VCS)
AUDIT.md
